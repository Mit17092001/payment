<?php
  header("location:http://localhost/php/myportfolio/suback.php")
  
  // session_start();
    // $email=$_SESSION['email'];
    // echo $_GET['id'];
    // $sql="SELECT * FROM `sub` WHERE `e-mail`='$email'";
    // $sp=mysqli_query($conn,$sql);
    // // $mpl="UPDATE `sub` SET `name`='name', WHERE `e-mail`=''";
?>